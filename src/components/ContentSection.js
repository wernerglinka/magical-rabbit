import styled from "@emotion/styled";

const ContentSection = styled.section`
  padding: 50px 0;

  h1 {
    font-size: 24px;
  }
  p {
    font-size: 16px;
    font-weight: 300;
  }
`;

export default ContentSection;
